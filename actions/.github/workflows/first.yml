name: shell commands 

on:
  push:
    branches:
      - 'main'

jobs:
    run-shell-commands:
      runs-on: ubuntu-latest
      steps:
        - name: echo string
          run: echo "Hello"
        - name: Multi-line string
          run: |
            pwd
            node -v
            ls -al
            df -h